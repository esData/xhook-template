name: Google Sheet
type: trigger
version: 1
summary: retrieve google worksheet, from first column as an accounts list
parameters:
  token:
    description: account id and secrets retreived from the vaults
    type: array[string]
    default: SUCCESS
test:
  - summary: unit test a single commit hook
    type: task
    parameters:
      reason: UNIT_TEST
