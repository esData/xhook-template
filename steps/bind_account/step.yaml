name: bind_account
type: step
version: 1
summary: continue only transactions was prepare/from hook account.
templates:
  main: main.c.mustache
  body:
    - bind_account.c.mustache
parameters:
  reason:
    description: Reason or references code
    type: string
    default: SUCCESS
test:
  - summary: unit test a single commit hook
    type: task
    parameters:
      reason: UNIT_TEST